import { bases, sauces, toppings } from '../data/pizza-options'

export default (state = [bases, sauces, toppings], { type, payload } = {}) => { //hier die local state's met pizza dingen die onchange gekozen zijn 
      return state
}